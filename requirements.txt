# 依赖真相源是 pyproject.toml + uv.lock，本文件仅供不用 uv 的 pip 用户，
# 两边改依赖时需手动同步。
# 仅推理/GUI 运行；导出/量化才需要的依赖见文件末尾注释块。

# ONNX 运行时二选一：默认 DirectML（任意显卡可用）；N 卡可换 onnxruntime-cuda
onnxruntime-directml>=1.24.4
# onnxruntime-cuda>=1.29.0

# 推理
gguf>=0.19.0
numpy>=2.5.2
scipy>=1.18.1
soundfile>=0.14.0
sounddevice>=0.5.6
tokenizers>=0.22

# GUI
ttkbootstrap>=2.2.2
windnd>=1.0.7

# ---- 以下仅导出/量化（1x/2x 号脚本）需要
# pip install torch>=2.10 torchaudio>=2.10 torchvision>=0.28.0 
# pip install transformers==4.57.6 accelerate==1.12.0 onnx>=1.22.0 onnxscript>=0.7.1 sentencepiece>=0.2.2 einops>=0.8.2
